apiVersion: v1
kind: Service
metadata:
  name: bontang-service
  labels:
    app: bontang-service
spec:
  type: ClusterIP
  selector:
    app: bontang-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 9001